apiVersion: v1
kind: Service
metadata:
  name: scm-dashboard
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: scm-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: {{ .Values.service.nodePort }}
  type: NodePort
