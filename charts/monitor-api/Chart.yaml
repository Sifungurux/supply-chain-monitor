apiVersion: v2
name: monitor-api
description: >-
  The main supply-chain-monitor API service: registers artifacts, runs
  scans (trivy/SBOM/SARIF/malware), and tracks pipeline stage history in
  Postgres. Deployed via Flux's HelmRelease (see
  k8s/monitor-api-helmrelease.yaml).
type: application
version: 0.1.0
appVersion: "dev"
