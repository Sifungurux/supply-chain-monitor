# Plain kustomize file (not a Flux CR) so `kubectl apply -k
# clusters/dev/flux-system/` applies gotk-sync.yaml in one command --
# matches the file `flux bootstrap` itself would generate here.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gotk-sync.yaml
