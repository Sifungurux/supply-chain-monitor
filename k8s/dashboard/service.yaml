apiVersion: v1
kind: Service
metadata:
  name: scm-dashboard
  namespace: supply-chain-monitor
spec:
  selector:
    app: scm-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30301
  type: NodePort
