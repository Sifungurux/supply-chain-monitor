# Dev-only API key for monitor-api's shared-key auth (see
# internal/api/router.go's withAuth and docs/architecture.md, "Adding
# API authentication"). "changeme-api-key" is a placeholder for a
# local, single-user test cluster -- generate a real random value
# (e.g. `openssl rand -hex 32`) and replace it, the same way
# postgres/secret.yaml's placeholder password should be replaced,
# before this points at anything shared.
apiVersion: v1
kind: Secret
metadata:
  name: scm-monitor-api-auth
  namespace: supply-chain-monitor
type: Opaque
stringData:
  API_KEY: qwe4r56789009876543223456789
