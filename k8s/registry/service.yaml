apiVersion: v1
kind: Service
metadata:
  name: scm-registry
  namespace: supply-chain-monitor
spec:
  selector:
    app: scm-registry
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30500
  type: NodePort
